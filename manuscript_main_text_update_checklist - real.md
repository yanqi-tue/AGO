# 正文表格与数据更新清单：全部模型剔除 fetal sex

本清单对照 clean version 手稿及本轮重新计算的结果整理，供人工修改正文使用。这里采用的口径是：**主判别结果**取 100 次重复分层 10 折交叉验证后每名受试者的平均折外预测，并按受试者进行 1,000 次 bootstrap；**校准、阈值及决策曲线**目前仅对第 0 次划分（seed=0）完成嵌套校准。下列阈值数字以 isotonic 为主要校准方法；Platt 结果另存作比较。

## 一、正文表格

| 位置 | 需要处理的内容                                                                                                                         |
| --- |---------------------------------------------------------------------------------------------------------------------------------|
| Table 1：人群特征 | 现有均值、人数和百分比不因模型剔除 fetal sex 而改变。`Fetal sex (Male)` 是描述性特征，可以保留；它不表示 fetal sex 仍是模型预测变量。表下注释需明确显著性标记依据原始 *p* 值还是 FDR 调整后的 *q* 值。 |
| Table 2：WHO 胎儿生长图比较 | WHO 各项 AUROC 数值可保留。删除两行平均值后的 `a/b/c` 显著性上标，以及 Mann–Whitney U 检验脚注。编辑明确要求移除这种比较；模型与 WHO 指标仅作描述性比较。                               |
| 新增四模型定义表 | 编辑要求逐项列出每个模型的预测变量及其可获得时间。当前候选变量数：“这里需要我手动输入”。若作为新的正文表，首页的 `Tables: 2` 及后续表号须同步调整。                                               |


## 二、摘要和结果段的 AUROC

以下是 100 次重复主分析的 AUROC（受试者 bootstrap 95% CI）：

| 结局 |                      M1 |                      M2 |                      M3 |                Combined |
| --- |------------------------:|------------------------:|------------------------:|------------------------:|
| ≥4000 g | **0.638 (0.612–0.661)** | **0.661 (0.635–0.684)** | **0.743 (0.719–0.765)** | **0.742 (0.717–0.764)** |
| ≥4500 g | **0.731 (0.680–0.780)** | **0.737 (0.685–0.786)** | **0.829 (0.785–0.874)** | **0.831 (0.788–0.876)** |

具体改动位置：

1. **摘要 Results：**最高 AUROC 的 `0.75` 改为约 **0.73**；`0.83` 改为约 **0.82**。结局人数 **505/6,371（7.9%）** 与 **77/6,371（1.2%）** 保持。
2. **结果 3.2 节，≥4000 g 段落：**原 M1 `0.66`、M2 `0.67`、M3/Combined `0.75`，均按上表替换。旧 `p<0.001` 不应直接沿用；若保留模型比较，应使用新配对分析及其 CI。现有 M3−M1 AUROC 差值约 **0.105（95% CI 0.081–0.128）**；M3−M2 约 **0.081（0.060–0.103）**。
3. **结果 3.2 节，≥4500 g 段落：**原 M1 `0.74`、M2 `0.75`、M3 `0.82`、Combined `0.83`，按上表替换。Combined 现在约 **0.82**。
4. **WHO 比较叙述：**WHO 指标本身不因去除 fetal sex 而更新，但“早孕 M1 与 Visit-3 WHO 指标相当”的判断应删除或明显收窄。新的 M1 AUROC 是 **0.63/0.72**，Visit-3 WHO 五项平均为 **0.66/0.74**，其中较强的单项指标更高。不要以五项 WHO AUROC 的平均值宣称与某一临床检测等效。

## 三、正文 AUPRC、Brier 和阈值结果

**AUPRC 与 Brier 目前来自不同的预测汇总，写作时必须分开说明。**若 AUPRC 与主分析及新版附图 S1 保持一致，Combined 模型为：

| 结局 | 100 次重复主分析 AUPRC（95% CI） | seed=0 isotonic Brier（95% CI） |
| --- |-------------------------:| ---: |
| ≥4000 g |  **0.209 (0.181–0.240)** | **0.0690 (0.0638–0.0739)** |
| ≥4500 g |  **0.066 (0.042–0.107)** | **0.01181 (0.00935–0.01429)** |

若正文希望 AUPRC 与 seed=0 isotonic 校准表完全同源，则该次划分的 Combined AUPRC 分别为 **0.190（0.163–0.220）** 和 **0.062（0.040–0.105）**。不能在同一行暗示这些指标都来自“100 次重复后的校准预测”。

原结果 3.2 节关于上五分位、上十分位的数字，若使用 seed=0 isotonic，应换成：

| 结局／模型 | 高风险组 | 捕获病例 |       敏感度 | 特异度 |       PPV | NPV |
| --- | --- | ---: |----------:| ---: |----------:| ---: |
| ≥4000 g／Combined | 上 20%，1,275 人 | **236/505** | **46.7%** | **82.3%** | **18.5%** | **94.7%** |
| ≥4000 g／Combined | 上 10%，638 人 | **142/505** | **30.1%** | **91.5%** | **24.3%** | **93.7%** |
| ≥4500 g／M3 | 上 20%，1,275 人 | **53/77** | **68.8%** | **80.6%** |  **4.2%** | **99.5%** |
| ≥4500 g／Combined | 上 20%，1,275 人 | **55/77** | **71.4%** | **80.6%** |  **4.3%** | **99.6%** |
| ≥4500 g／M3 | 上 10%，638 人 | **36/77** | **46.8%** | **90.4%** |  **5.6%** | **99.3%** |
| ≥4500 g／Combined | 上 10%，638 人 | **33/77** | **42.9%** | **90.4%** |  **5.2%** | **99.2%** |


## 四、LGA 敏感性分析在正文中的数值

LGA90 **1,319 人（20.7%）**、LGA97 **470 人（7.4%）**的人数不变。结果段的 AUROC 改为：

| 结局 | M1 AUROC（95% CI） | M3 AUROC（95% CI） |
| --- | ---: | ---: |
| LGA90 | **0.620 (0.604–0.637)** | **0.731 (0.716–0.745)** |
| LGA97 | **0.651 (0.626–0.675)** | **0.758 (0.734–0.780)** |

本轮尚未运行 LGA90/LGA97 的新校准流程。正文暂不能声称新版附表 S4 和附图 S3 已经提供与去除 fetal sex 后模型一致的校准结果。

## 五、与结果同步修改的方法和解释

1. **摘要 Methods 与 2.3 节预测变量：**删除 fetal sex 作为模型输入的描述。LGA90/LGA97 结局仍依据出生时性别和胎龄计算百分位，这段定义保留；Table 1 的 fetal sex 描述行也可保留。
2. **2.5 节模型定义：**明确 M3 为 M1＋Visit 3，Combined 才包含 Visit 2 和 Visit 3。模型候选变量个数分别为 **“这里我手动输入”**。
3. **2.5 节验证流程：**旧文的 `1,000 次`重复交叉验证应改为实际的 **100 次重复、每次 10 折**。同一受试者的 100 个折外预测先取平均，然后对受试者进行 **1,000 次 bootstrap**。本轮 LR 固定参数为 `C=0.1`、`l1_ratio=0.3`；不能继续写成已在内层搜索惩罚和正则化强度。阴性类别权重为 **1**，阳性在 ≥4000 g、LGA90、LGA97 为 **5**，在 ≥4500 g 为 **10**；旧稿的“按阴阳性比例平方根逐折计算权重”不符。


## 六、投稿前必须核实的变量问题


- ≥4500 g 只有 **77 个事件**。Combined 有 **42 个候选预测变量、43 个含截距参数**，每个候选预测变量约 **1.83 个事件**。现有“平均非零系数数目”不等同于正式的有效自由度或 Riley 样本量评估，不能这样描述。

## 数据来源


本清单不修改手稿文件本身。Figure 4–6 按此前约定保持不动；非线性模型比较 S9 放到后一阶段；有限的荷兰队列分析 S13 暂不处理。
